   # check of space-expression ignoring

f1 := true       # is the same as '1'
f2 := false      # is the same as '0'

true := 1     # should be error as 'true' is reserved
false := 0    # should be error as 'false' is reserved

g1(x) := (x + 1
g2(x) :=  x + 1)
g3(x) := ) x + 1(
g4(x) := (x + 1)
g5(x) := ((((((x))) + (((1))))))
